const at = (t) => {
    return `${t}\n`
}

// var input = ''
// input += at('')
// input += at('')
// input += at('')
// input += at('')

var input = require('fs').readFileSync('/dev/stdin', 'utf8')
var lines = input.split('\n')
var lines = input.split('\n')